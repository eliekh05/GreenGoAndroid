<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Shell theme — Compose handles all UI.
        NoActionBar removes the legacy action bar.
        enableEdgeToEdge() in MainActivity handles the rest.
    -->
    <style name="Theme.GreenGo" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
